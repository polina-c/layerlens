name: example
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.21.0
  layerlens:
    path: ../
