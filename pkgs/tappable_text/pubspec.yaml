name: tappable_text
description: "Flutter package for creating consistently looking tappable texts across the app."
version: 0.0.1-dev001
homepage:

resolution: workspace

environment:
  sdk: ^3.12.0-285.0.dev
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
